import 'package:flutter/material.dart';
import 'package:pet_adoption_assignment/features/settings/domain/models/enum_values.dart';

const appThemeModes = EnumValues(
  {
    'light': ThemeMode.light,
    'dark': ThemeMode.dark,
    'system': ThemeMode.system,
  },
);
